# 快速使用指南 - 训练数据验证和清理

## 🎯 目标
验证训练数据集中的Lean代码,自动删除不正确的数据,保留可用于训练的高质量数据。

## 📝 快速步骤

### 1️⃣ 先测试少量样本 (推荐)

```powershell
# 方式1: 使用测试脚本(验证前5个样本)
python test_validation_clean.py

# 方式2: 使用PowerShell脚本验证前10个样本
.\run_validation_and_clean.ps1 -MaxSamples 10
```

### 2️⃣ 验证全部数据

```powershell
# 验证所有数据(默认配置)
.\run_validation_and_clean.ps1

# 或指定参数
.\run_validation_and_clean.ps1 -InputFile "data/step3_consensus_v2/enhanced_consensus.jsonl" `
                                -OutputFile "data/validated/consensus_valid.jsonl" `
                                -Timeout 60
```

### 3️⃣ 查看结果

验证完成后会生成3个文件:

```
data/validated/
  ├── consensus_valid.jsonl          # ✅ 有效数据(用于训练)
  ├── consensus_valid_errors.jsonl   # ❌ 错误详情
  └── consensus_valid_report.txt     # 📊 统计报告
```

## 📊 预期结果

```
================================================================================
验证完成!
================================================================================
总样本数: 996
✓ 有效样本: 723 (72.59%)
✗ 无效样本: 273 (27.41%)
  - 无代码: 12
  - 编译错误: 261
================================================================================
```

## ⚙️ 常用命令

```powershell
# 仅验证前100个样本
.\run_validation_and_clean.ps1 -MaxSamples 100

# 调试模式(显示详细错误)
.\run_validation_and_clean.ps1 -MaxSamples 10 -Debug

# 增加超时时间(适用于复杂证明)
.\run_validation_and_clean.ps1 -Timeout 120

# 不保存错误详情(节省空间)
.\run_validation_and_clean.ps1 -NoSaveInvalid
```

## ❓ 常见问题

**Q: 验证很慢?**  
A: 第一次运行会构建Lean缓存,后续会快很多。

**Q: 某些样本超时?**  
A: 使用 `-Timeout 120` 增加超时时间。

**Q: 如何查看被删除的数据?**  
A: 查看 `*_errors.jsonl` 文件。

## 📚 详细文档

查看完整文档: [VALIDATION_CLEANING_README.md](VALIDATION_CLEANING_README.md)

---

**重要**: 务必在训练前验证数据,以确保数据质量! ✅
